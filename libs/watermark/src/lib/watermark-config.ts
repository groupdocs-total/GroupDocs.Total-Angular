export class WatermarkConfig {
  rewrite: boolean;
  upload: boolean;
  browse: boolean;
  download: boolean;

  filesDirectory: string;
  preloadPageCount: 0;
  zoom: boolean;

  textWatermark: boolean;
  imageWatermark: boolean;
}
