import { DndWatermarkDirective } from './dnd-watermark.directive';

describe('DndWatermarkDirective', () => {
  it('should create an instance', () => {
    const directive = new DndWatermarkDirective();
    expect(directive).toBeTruthy();
  });
});
