import { DisabledCursorDirective } from './disabled-cursor.directive';

describe('DisabledCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledCursorDirective();
    expect(directive).toBeTruthy();
  });
});
