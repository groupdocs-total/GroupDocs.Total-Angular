import { RenderPrintDirective } from './render-print.directive';

describe('RenderPrintDirective', () => {
  it('should create an instance', () => {
    //const directive = new RenderPrintDirective();
    //expect(directive).toBeTruthy();
  });
});
