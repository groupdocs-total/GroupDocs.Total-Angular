import { RotationDirective } from './rotation.directive';

describe('RotationDirective', () => {
  it('should create an instance', () => {
    const directive = new RotationDirective();
    expect(directive).toBeTruthy();
  });
});
