import { EditHeaderFooterDirective } from './editheaderfooter.directive';

describe('EditHeaderFooterDirective', () => {
  it('should create an instance', () => {
    //const directive = new EditHeaderFooterDirective();
    //expect(directive).toBeTruthy();
  });
});
