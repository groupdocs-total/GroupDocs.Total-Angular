import { TermNavigationDirective } from './term-navigation.directive';

describe('TermNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new TermNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
